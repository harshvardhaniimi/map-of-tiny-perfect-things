[build]
  command = "CI= npm run build"
  publish = "build/"

[build.environment]
  NODE_VERSION = "22.12.0"

[build.processing]
  ignore_html_forms = false

[functions]
  directory = "netlify/functions"
